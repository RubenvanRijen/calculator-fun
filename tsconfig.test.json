{
  /* Type-checks the tests and the tooling configs alongside the sources. */
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "types": ["vitest/globals", "node"]
  },
  "include": ["ts/**/*.ts", "e2e/**/*.ts", "vite.config.ts", "vitest.config.ts"],
  "exclude": []
}
